// Basic smoke test placeholder. Substantive coverage is the Rust engine's Tier-1 suite
// (cargo test); the Flutter shell is a thin presentation layer.
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
